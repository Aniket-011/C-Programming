/*
#include<stdio.h>
 //void sum ();------- Here we not declare function but it also give result b/c complier always take integer return type
                        // but if we declare char type function at function body then it will give error.
int main(){
    sum();
    return 0;
}
void sum(){ //Here we use void b/c not use return any value.
    int a,b, sum=0;
    printf("Enter two number");
    scanf("%d %d",&a,&b);
    sum=a+b;
    printf("Sum is %d",sum);
    
}
*/